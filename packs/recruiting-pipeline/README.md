# animus-pack-recruiting-pipeline

**Moved.** This reference pack now lives at its own repo:

→ **https://github.com/launchapp-dev/animus-pack-recruiting-pipeline**

Install via:

```
animus pack install launchapp-dev/animus-pack-recruiting-pipeline@v0.1.0
```

Or clone for inspection / forking:

```
git clone https://github.com/launchapp-dev/animus-pack-recruiting-pipeline
```

This stub will be removed entirely in a future ao-cli release once the
`animus pack install` flow + marketplace discovery are wired up. For
now it's kept as a redirect for anyone browsing the ao-cli tree.

See the parent index at [`packs/README.md`](../README.md) for the full
list of reference packs.
