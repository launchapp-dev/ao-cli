{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Subject",
  "description": "A normalized cross-backend representation of a unit of dispatchable work.\n\nSubjects flow from backends into the daemon's dispatch queue and back as\nupdates after a workflow run completes. Backend-specific fields the\ndaemon doesn't interpret live in [`Subject::custom`] and are addressable\nfrom workflow YAML via templating (e.g.\n`{{subject.custom.story_points}}`).",
  "type": "object",
  "properties": {
    "assignee": {
      "description": "Free-form assignee identifier. Format is backend-specific; commonly\na username, email, or `\"agent:<name>\"` for an Animus agent.",
      "type": [
        "string",
        "null"
      ]
    },
    "children": {
      "description": "Child subjects, if any.",
      "type": "array",
      "items": {
        "$ref": "#/$defs/SubjectId"
      }
    },
    "created_at": {
      "description": "When the subject was first created in its native system.",
      "type": "string",
      "format": "date-time"
    },
    "custom": {
      "description": "Backend-specific fields the daemon does not interpret. Workflows\ncan read these via templating.",
      "type": "object",
      "additionalProperties": true
    },
    "description": {
      "description": "Long-form description (markdown encouraged).",
      "type": [
        "string",
        "null"
      ]
    },
    "id": {
      "description": "Backend-qualified identifier. See [`SubjectId`].",
      "$ref": "#/$defs/SubjectId"
    },
    "kind": {
      "description": "Subject kind. Backend-defined. Examples: `\"task\"`, `\"issue\"`,\n`\"epic\"`, `\"ticket\"`, `\"document\"`, `\"lead\"`, `\"contract\"`,\n`\"incident\"`.",
      "type": "string"
    },
    "labels": {
      "description": "Labels/tags. Backend-defined string set.",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "parent": {
      "description": "Parent subject, if any (e.g. an epic for an issue).",
      "anyOf": [
        {
          "$ref": "#/$defs/SubjectId"
        },
        {
          "type": "null"
        }
      ]
    },
    "priority": {
      "description": "Optional priority on a 0..=4 scale: 0 = none, 1 = low, 2 = medium,\n3 = high, 4 = critical.",
      "type": [
        "integer",
        "null"
      ],
      "format": "uint8",
      "maximum": 255,
      "minimum": 0
    },
    "status": {
      "description": "Normalized status. See [`SubjectStatus`].",
      "$ref": "#/$defs/SubjectStatus"
    },
    "title": {
      "description": "Short human-readable title.",
      "type": "string"
    },
    "updated_at": {
      "description": "When the subject was last updated in its native system.",
      "type": "string",
      "format": "date-time"
    },
    "url": {
      "description": "Permalink to the subject in its native system, if one exists.",
      "type": [
        "string",
        "null"
      ]
    }
  },
  "required": [
    "id",
    "kind",
    "title",
    "status",
    "created_at",
    "updated_at"
  ],
  "$defs": {
    "SubjectId": {
      "description": "Backend-qualified identifier for a subject.\n\nConvention is `\"<backend>:<native_id>\"`, e.g. `\"linear:ENG-123\"`,\n`\"jira:PROJ-456\"`, `\"github:owner/repo#789\"`, `\"native:TASK-001\"`. The\ndaemon treats the value as opaque; only the originating backend\ninterprets the native portion.\n\nThe `backend:` prefix is reserved. Plugin authors should always emit\nprefixed ids so cross-backend collisions are impossible.",
      "type": "string"
    },
    "SubjectStatus": {
      "description": "Normalized cross-backend subject status.\n\nBackend-native states (`\"Backlog\"`, `\"In Review\"`, `\"Won't Fix\"`, ...) map\ninto one of these five via the `status_map` declared per-subject in\nworkflow YAML. The mapping lives in configuration, not code, so adding a\nnew backend never requires extending this enum.",
      "oneOf": [
        {
          "description": "Eligible for dispatch.",
          "type": "string",
          "const": "ready"
        },
        {
          "description": "Currently being worked by a workflow run (or by a human, in the\nupstream system).",
          "type": "string",
          "const": "in-progress"
        },
        {
          "description": "Cannot proceed; awaiting unblock.",
          "type": "string",
          "const": "blocked"
        },
        {
          "description": "Successfully completed.",
          "type": "string",
          "const": "done"
        },
        {
          "description": "Abandoned without completion.",
          "type": "string",
          "const": "cancelled"
        }
      ]
    }
  }
}
