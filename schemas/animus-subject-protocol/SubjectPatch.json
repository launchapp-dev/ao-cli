{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "SubjectPatch",
  "description": "A patch applied to a subject via `subject/update`.\n\nAll fields are optional. Missing fields are not modified. The\ndouble-`Option` on [`SubjectPatch::assignee`] distinguishes \"not modified\"\n(`None`) from \"explicitly clear\" (`Some(None)`). Labels are partitioned\ninto add/remove sets to avoid lost-write races on the labels list as a\nwhole.",
  "type": "object",
  "properties": {
    "assignee": {
      "description": "Set, change, or clear the assignee. `Some(None)` means clear.",
      "type": [
        "string",
        "null"
      ]
    },
    "comment": {
      "description": "Optional comment to post alongside the update. Backends that don't\nsupport comments may surface this as a summary in their native\nactivity log.",
      "type": [
        "string",
        "null"
      ]
    },
    "custom": {
      "description": "Backend-specific custom fields to merge. An explicit JSON `null`\nvalue clears the field at that key.",
      "type": "object",
      "additionalProperties": true
    },
    "labels_add": {
      "description": "Labels to add (deduplicated against existing labels).",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "labels_remove": {
      "description": "Labels to remove.",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "status": {
      "description": "Set the normalized status. Backends translate to their native value\nusing the workflow-YAML `status_map`.",
      "anyOf": [
        {
          "$ref": "#/$defs/SubjectStatus"
        },
        {
          "type": "null"
        }
      ]
    }
  },
  "$defs": {
    "SubjectStatus": {
      "description": "Normalized cross-backend subject status.\n\nBackend-native states (`\"Backlog\"`, `\"In Review\"`, `\"Won't Fix\"`, ...) map\ninto one of these five via the `status_map` declared per-subject in\nworkflow YAML. The mapping lives in configuration, not code, so adding a\nnew backend never requires extending this enum.",
      "oneOf": [
        {
          "description": "Eligible for dispatch.",
          "type": "string",
          "const": "ready"
        },
        {
          "description": "Currently being worked by a workflow run (or by a human, in the\nupstream system).",
          "type": "string",
          "const": "in-progress"
        },
        {
          "description": "Cannot proceed; awaiting unblock.",
          "type": "string",
          "const": "blocked"
        },
        {
          "description": "Successfully completed.",
          "type": "string",
          "const": "done"
        },
        {
          "description": "Abandoned without completion.",
          "type": "string",
          "const": "cancelled"
        }
      ]
    }
  }
}
