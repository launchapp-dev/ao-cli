# Animus plugin SDK (typescript)

**Moved.** The typescript SDK now lives at its own repo:

→ **https://github.com/launchapp-dev/animus-plugin-sdk-ts**

This stub will be removed entirely in a future ao-cli release. The SDK
is published to its language ecosystem registry — see the upstream repo
for current install instructions.
